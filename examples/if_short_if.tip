func main() int {
    var x,y,z int;
    x = input;
    y = input;
    z = input;
    if (x == 0)
        if (y == 0)
            output z;
        else
            output x;
    return y;
}
