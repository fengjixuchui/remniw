func idf(a int) int {
    return a;
}

func posf(b int) int {
    return b;
}

func negf(c int) int {
    return c;
}

func main() int {
    var pos, neg, top, zero int;

    zero = 0;

    top = idf(5);

    top = idf(-4);

    pos = posf(5);

    pos = posf(7);

    neg = negf(-4);

    neg = negf(-8);

    return 0;
}