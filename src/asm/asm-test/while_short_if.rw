// RUN: %remniw %s -o %t ; echo 1 0 1 | lli %t | FileCheck %s
// RUN: %remniw %s -o %t -debug ; echo 1 0 1 | lli %t | FileCheck %s

func main() int {
    var x,y,z int;
    x = 1;
    y = 0;
    z = 20;
    if (x == 1) {
        while (x > 0) {
            if (y == 0) {
                z = z + 1;
            }
            else {
                x = 0;
            }
            x = x - 1;
        }
    }
    return z;
}

// CHECK: 1