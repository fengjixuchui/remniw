// RUN: %remniw %s -o %t ; lli %t | FileCheck %s
// RUN: %remniw %s -o %t -debug ; lli %t | FileCheck %s

func main() int {
    var a,b int;
    var c *int;

    a = 18;
    b = 17 + (a / 3);

    {
        a = 5+2;
        c = &a;
    }

    a = *c;

    a = (a/2) / 3; // a=1

    return a;
}
