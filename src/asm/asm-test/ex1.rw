// RUN: %remniw %s -o %t ; lli %t | FileCheck %s
// RUN: %remniw %s -o %t -debug ; lli %t | FileCheck %s

func f(x int) int { return x; }
func main() int {
    var t func(int) int;
    var a, b int;
    t = f;
    a = t(1); // a = 1
    return a;
}

// CHECK: 1