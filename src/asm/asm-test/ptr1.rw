// RUN: %remniw %s -o %t ; lli %t | FileCheck %s
// RUN: %remniw %s -o %t -debug ; lli %t | FileCheck %s

func test(p *int) int {
    var q int;
    q=3;
    output (*p);
    return 0;
}

func main() int {
    var a int;
    var b *int;
    a = 1;
    b = &a;
    output test(b);
    return 0;
}

// CHECK: 1
// CHECK: 0