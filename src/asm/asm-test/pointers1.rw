// RUN: %remniw %s -o %t ; lli %t | FileCheck %s
// RUN: %remniw %s -o %t -debug ; lli %t | FileCheck %s

func main() int {
  var x int;
  var pa, pb *int;
  x = 5;
  pa = &x;
  pb = pa;
  *pb = 2;
  if (x == 2) {
    output 1;
  } else {
    output 0;
    output x;
  }
  return 0;
}

// CHECK: 1