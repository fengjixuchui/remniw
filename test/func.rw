// RUN: %remniw %s -o %t ; echo 10 10 | lli %t | FileCheck %s

func f(a int,b int) int {
    return g(a,b);
}


func g(a int,b int) int {
    return a+b;
}

func main() int {
    var a,b int;

    a = input;
    b = input;

    output f(a,b);
    return 0;
}

// CHECK: 20