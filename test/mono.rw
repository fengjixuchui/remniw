// RUN: %remniw %s -o %t ; echo 2021920 | lli %t | FileCheck %s

func foo(x int,y int) int {
     x = 2*y;
     return x+1;
}

func main() int {
    var a,b int;
    a = input;
    b = foo(a,17);
    output b;
    return 0;
}

// CHECK: 35