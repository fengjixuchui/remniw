// RUN: %remniw %s -o %t ; echo 1234 | lli %t | FileCheck %s

func main() int {
    var x,y,z int;
    x=27;
    y=input;
    z=2*x+y;
    if( 0>x ){
        y=z-3;
    } else {
        y=12;
    }
    output y;
    return 0;
}

// CHECK: 12