// RUN: %remniw %s -o %t ; lli %t | FileCheck %s

func main() int {
    var x int;
    var y *int;
    x = 10;
    y = &x;
    while (*y > 0) {
        *y = *y-1;
    }
    output x;
    return 0;
}

// CHECK: 0